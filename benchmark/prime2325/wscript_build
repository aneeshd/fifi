#! /usr/bin/env python
# encoding: utf-8

bld.program(
    features = 'cxx',
    source   = ['main.cpp'],
    target   = 'fifi_prime2325_benchmarks',
    cxxflags = bld.toolchain_cxx_flags() + ['-std=gnu++0x'],
    use      = ['gtest','fifi_includes', 'boost_includes',
                'boost_chrono', 'boost_system',
                'sak_includes'])
