#! /usr/bin/env python
# encoding: utf-8

import glob


bld.program(
    features = 'cxx',
    source   = ['main.cpp'],
    target   = 'fifi_basic_operations_benchmarks',
    includes = 'fifi_dev/fifi',
    use      = ['BOOST','PLATY','SPEEDY'],
    install_path = 'bin'+bld.env['TOOLCHAIN']
)
